---
/**
 * Startseite ATG Carsharing — Durchstich in neuer CI (Rot/Gelb aus Logo & Fahrzeugen).
 * Sektionen: Hero · Stat-Leiste · So geht's · Flotte · Preise+Kostenrechner · Kooperation · FAQ · CTA.
 * Fahrzeugfotos: aktuell von der Live-Seite referenziert → Phase 2 lokal + WebP/AVIF + srcset.
 */
import Base from '../layouts/Base.astro';
import StandorteKarte from '../components/StandorteKarte.astro';

const base = import.meta.env.BASE_URL;
const heroPhoto = 'https://www.atg-grasbrunn.de/wp-content/uploads/2024/09/WhatsApp-Bild-2024-09-01-um-12.45.09_ef67264d-1.jpg';
const BOOK = 'https://www.atg-grasbrunn.de/mitgliederbereich/';

// Hero-Slider: echte Fahrzeugfotos (KFZ). Jeder Slide hat eine eigene Botschaft
// (h1 zweizeilig, 2. Zeile = gelber Akzent) + Lead + Tag.
// pos = background-position (verhindert „zu groß"/abgeschnitten wirkende Motive).
const heroSlides = [
  {
    kind: 'kfz', img: heroPhoto, pos: 'center 55%',
    h1a: 'Weniger Autos.', h1b: 'Dennoch mobil.',
    lead: 'Teil dir ein Auto, statt eins zu besitzen — vom E-Auto bis zum 9-Sitzer Kleinbus, direkt um die Ecke. <strong>Ohne Monatsbeitrag.</strong>',
    tag: 'Renault ZOE — emissionsfrei · ab 1 €/h zzgl. km · Sprit inklusive',
  },
  {
    kind: 'kfz', img: 'https://www.atg-grasbrunn.de/wp-content/uploads/2022/03/20220322_161630.jpg', pos: 'center 50%',
    h1a: 'Ganze Gruppe?', h1b: 'Alle rein.',
    lead: 'Der 9-Sitzer Kleinbus bringt Verein, Familie oder Freunde gemeinsam ans Ziel — echte Sitzplätze statt Gedränge.',
    tag: 'Renault Trafic — 9-Sitzer Kleinbus für Gruppen & Ausflüge',
  },
  {
    kind: 'kfz', img: 'https://www.atg-grasbrunn.de/wp-content/uploads/2022/12/20221204_151256.jpg', pos: 'center 55%',
    h1a: 'Einer für', h1b: 'jeden Weg.',
    lead: 'Kombi, Kleinwagen oder E-Auto — fünf Fahrzeugklassen an festen Stellplätzen in Neukeferloh.',
    tag: 'Renault Clio Grandtour — Kombi für Einkauf & Ausflug',
  },
];

// Führerscheinklasse: alle Fahrzeuge ≤ 3,5 t und ≤ 9 Sitze → Klasse B (alt: Klasse 3).
// ISOFIX: alle Fahrzeuge haben ISOFIX-Halterungen für Kindersitze.
// ahk (Anhängerkupplung): null = noch bestätigen (Angabe folgt vom Verein).
const cars = [
  {
    slug: 'zoe', name: 'Renault ZOE', type: 'Elektro · emissionsfrei', badge: 'E-Auto',
    loc: 'Am Rathaus, Neukeferloh', seats: '5 Sitze', tarif: 'ab 0,45 €/km',
    antrieb: 'Elektro', schaltung: 'Automatik', isofix: true, fuehrerschein: 'Klasse B (alt: 3)', ahk: null,
    img: heroPhoto,
  },
  {
    slug: 'corsa', name: 'Opel Corsa', type: 'Wendiger Stadtflitzer', badge: 'Kleinwagen',
    loc: 'Finkenstraße, hinter der Kirche', seats: '5 Sitze', tarif: 'ab 0,40 €/km',
    antrieb: 'Benzin', schaltung: 'Schaltgetriebe', isofix: true, fuehrerschein: 'Klasse B (alt: 3)', ahk: null,
    img: 'https://www.atg-grasbrunn.de/wp-content/uploads/2025/07/Opel-Corsa-NEU.jpg',
  },
  {
    slug: 'sandero', name: 'Dacia Sandero', type: 'Günstig & praktisch', badge: 'Kleinwagen',
    loc: 'Ecke Waldstraße / Saarlandstraße', seats: '5 Sitze', tarif: 'ab 0,40 €/km',
    antrieb: 'Benzin', schaltung: 'Schaltgetriebe', isofix: true, fuehrerschein: 'Klasse B (alt: 3)', ahk: null,
    img: 'https://www.atg-grasbrunn.de/wp-content/uploads/2025/07/Dacia-Sandero-NEU-1.jpg',
  },
  {
    slug: 'clio', name: 'Renault Clio Grandtour', type: 'Kombi mit großem Kofferraum', badge: 'Kombi',
    loc: 'Dianastraße (beim Orterer), Neukeferloh', seats: '5 Sitze · großer Kofferraum', tarif: 'ab 0,45 €/km',
    antrieb: 'Benzin', schaltung: 'Schaltgetriebe', isofix: true, fuehrerschein: 'Klasse B (alt: 3)', ahk: null,
    img: 'https://www.atg-grasbrunn.de/wp-content/uploads/2022/12/20221204_151256.jpg',
  },
  {
    slug: 'trafic', name: 'Renault Trafic', type: 'Kleinbus — 9 Sitze für Gruppen & Ausflüge', badge: 'Kleinbus · 9 Sitze',
    loc: 'Neukeferloh', seats: '9 Sitze', tarif: 'ab 0,60 €/km',
    antrieb: 'Diesel', schaltung: 'Schaltgetriebe', isofix: true, fuehrerschein: 'Klasse B (alt: 3)', ahk: null,
    img: 'https://www.atg-grasbrunn.de/wp-content/uploads/2022/03/20220322_161630.jpg',
  },
];

// Kompakte Ausstattungs-Chips je Fahrzeug (für Galerie-Slide „Ausstattung").
const equip = (c) => [
  { ic: 'seat', t: c.seats },
  { ic: 'gear', t: c.schaltung },
  { ic: 'fuel', t: c.antrieb },
  { ic: 'child', t: 'ISOFIX' },
  { ic: 'licence', t: c.fuehrerschein },
  { ic: 'trailer', t: c.ahk === true ? 'mit AHK' : c.ahk === false ? 'ohne AHK' : 'AHK auf Anfrage' },
];

// Inline-SVG-Icons (aria-hidden, dekorativ). Lucide-Stil, stroke=currentColor.
const S = 'viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"';
const icons = {
  pin: `<svg ${S}><path d="M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0z"/><circle cx="12" cy="10" r="3"/></svg>`,
  seat: `<svg ${S}><path d="M19 9V6a2 2 0 0 0-2-2H7a2 2 0 0 0-2 2v3"/><path d="M3 15a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2v3"/><path d="M5 18v2M19 18v2"/></svg>`,
  gear: `<svg ${S}><path d="M20 7h-9"/><path d="M14 17H5"/><circle cx="17" cy="17" r="3"/><circle cx="7" cy="7" r="3"/></svg>`,
  fuel: `<svg ${S}><line x1="3" y1="22" x2="15" y2="22"/><line x1="4" y1="9" x2="14" y2="9"/><path d="M14 22V4a2 2 0 0 0-2-2H6a2 2 0 0 0-2 2v18"/><path d="M14 13h2a2 2 0 0 1 2 2v2a2 2 0 0 0 4 0V9.83a2 2 0 0 0-.59-1.42L18 5"/></svg>`,
  child: `<svg ${S}><circle cx="12" cy="12" r="9"/><path d="m8.5 12.5 2 2 4-4.5"/></svg>`,
  licence: `<svg ${S}><rect x="3" y="5" width="18" height="14" rx="2"/><circle cx="9" cy="12" r="2"/><path d="M14 10h4M14 14h3"/></svg>`,
  trailer: `<svg ${S}><rect x="2" y="8" width="14" height="8" rx="1"/><path d="M16 14h2"/><circle cx="20" cy="16" r="2"/></svg>`,
};

const steps = [
  { n: '1', h: 'Online melden', t: 'Kurzes Formular ausfüllen. Wir melden uns mit allen Infos zum Verein und zur Carsharing-Teilnahme.' },
  { n: '2', h: 'Unterlagen & Einlage', t: 'Ausweis und Führerschein nachreichen, einmalig 50 € Aufnahme und 600 € Einlage (bei Austritt zurück).' },
  { n: '3', h: 'Einweisung & losfahren', t: 'Kurze Einweisung vor Ort, Transponderkarte erhalten — ab dann buchst du jedes freie Fahrzeug selbst.' },
];

const faqs = [
  { q: 'Was kostet Carsharing bei ATG?', a: 'Tagsüber ab 1,00 €/Std., nachts ab 0,20 €/Std. — jeweils zzgl. Kilometer ab 0,40 €/km je nach Fahrzeug. Der Sprit ist inklusive: getankt wird mit der Tankkarte im Fahrzeug, du zahlst und verauslagst nichts. Einmalig 50 € Aufnahme und 600 € Einlage (bei Austritt zurück) — je Haushalt einmal, egal wie viele Fahrer. Kein Monatsbeitrag.' },
  { q: 'Wie werde ich Mitglied?', a: 'Online melden, Ausweis und Führerschein nachreichen, Einlage zahlen, kurze Einweisung vor Ort — dann buchst du selbstständig. Je Haushalt genügt eine Mitgliedschaft mit einer Einlage — alle Fahrer im Haushalt sind mit dabei.' },
  { q: 'Wo stehen die Fahrzeuge?', a: 'An festen Stellplätzen in Neukeferloh (u.a. Rathaus, Finkenstraße, Waldstraße/Saarlandstraße). Über die Kooperation mit den Vaterstettener Auto-Teilern kannst du zusätzlich deren Fahrzeuge nutzen.' },
  { q: 'Ist der Sprit im Preis enthalten?', a: 'Ja. Sprit und Versicherung sind in den Kilometerpreisen bereits enthalten. Die Tankkarte liegt im Fahrzeug — du zahlst keinen Cent extra fürs Tanken.' },
  { q: 'Gibt es einen Monatsbeitrag?', a: 'Nein. Nur die einmalige Einlage (rückzahlbar) und der Aufnahmebeitrag. Bezahlt wird nur, was tatsächlich gefahren wird.' },
];

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((f) => ({ '@type': 'Question', name: f.q, acceptedAnswer: { '@type': 'Answer', text: f.a } })),
};
---

<Base>
  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} is:inline />

  <!-- ============ HERO (Kino-Slider) ============ -->
  <section class="hero" id="top" aria-label="Carsharing in Grasbrunn">
    <div class="hero-bg" aria-hidden="true">
      {heroSlides.map((s, i) => (
        <div
          class:list={['hero-slide', { 'is-active': i === 0, 'hero-slide-mvv': s.kind === 'mvv' }]}
          data-h1a={s.h1a}
          data-h1b={s.h1b}
          data-lead={s.lead}
          data-tag={s.tag}
          style={`background-image:url(${s.img || `${base}img/mvv-motiv.svg`}); background-position:${s.pos};`}
        ></div>
      ))}
    </div>
    <div class="hero-scrim" aria-hidden="true"></div>

    <div class="wrap hero-inner">
      <p class="hero-kicker"><span class="dot"></span> Carsharing dahoam · Grasbrunn &amp; Umgebung</p>
      <h1 class="hero-h1" id="hero-h1">
        <span class="h1a" id="hero-h1a">{heroSlides[0].h1a}</span><br />
        <span class="hl" id="hero-h1b">{heroSlides[0].h1b}</span>
      </h1>
      <p class="hero-lead" id="hero-lead" set:html={heroSlides[0].lead}></p>
      <p class="hero-tag" id="hero-tag">{heroSlides[0].tag}</p>
      <div class="cta-row">
        <a class="btn btn-red" href="#mitglied">Mitglied werden</a>
        <a class="btn btn-yellow" href={BOOK} rel="noopener">Fahrzeug buchen</a>
      </div>
      <ul class="trust">
        <li><span class="ck">✓</span> 5 Fahrzeuge direkt in Neukeferloh</li>
        <li><span class="ck">✓</span> Kein Monatsbeitrag</li>
        <li><span class="ck">✓</span> Versicherung &amp; Sprit inklusive</li>
      </ul>
    </div>

    <div class="hero-dots" role="tablist" aria-label="Fahrzeug-Motive wechseln">
      {heroSlides.map((s, i) => (
        <button
          class:list={['hero-dot', { 'is-on': i === 0 }]}
          type="button"
          role="tab"
          aria-selected={i === 0 ? 'true' : 'false'}
          aria-label={`Motiv ${i + 1}`}
        ></button>
      ))}
    </div>

    <a class="hero-scrollcue" href="#so-gehts" aria-label="Weiter zu: So geht's"><span></span></a>
  </section>

  <!-- ============ STAT-LEISTE ============ -->
  <section class="stats">
    <div class="wrap stats-grid">
      <div class="stat"><b data-count="5">5</b><span>Fahrzeuge in Neukeferloh</span></div>
      <div class="stat"><b>0&nbsp;€</b><span>Monatsbeitrag · Sprit inkl.</span></div>
      <div class="stat"><b>ab&nbsp;1&nbsp;€/h</b><span>tagsüber · zzgl. km</span></div>
      <div class="stat"><b>ab&nbsp;0,20&nbsp;€/h</b><span>nachts · zzgl. km</span></div>
    </div>
  </section>

  <!-- ============ SO GEHT'S ============ -->
  <section id="so-gehts" class="sec sec-white">
    <div class="wrap">
      <header class="sec-head reveal">
        <h2>In 3 Schritten Mitglied — und losgefahren</h2>
        <p>Kein kompliziertes Verfahren. Du meldest dich online, wir lernen uns kurz vor Ort kennen, dann fährst du.</p>
      </header>
      <div class="steps">
        {steps.map((s) => (
          <article class="step">
            <div class="step-num">{s.n}</div>
            <h3>{s.h}</h3>
            <p>{s.t}</p>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- ============ FLOTTE ============ -->
  <section id="flotte" class="sec">
    <div class="wrap">
      <header class="sec-head reveal">
        <h2>Fünf Fahrzeuge für jeden Zweck</h2>
        <p>Vom kleinen Stadtflitzer übers E-Auto bis zum 9-Sitzer Kleinbus — alle an festen Stellplätzen in Neukeferloh.</p>
      </header>
      <div class="cars">
        {cars.map((c) => (
          <article class="car" data-car={c.slug}>
            <div class="car-gallery" data-gallery aria-label={`Galerie ${c.name}: Fahrzeug, Standort, Ausstattung`}>
              <figure class="cg-slide cg-photo is-on" style={`background-image:url(${c.img})`}>
                <span class="car-badge">{c.badge}</span>
                <figcaption class="cg-cap">Das Fahrzeug</figcaption>
              </figure>
              <figure class="cg-slide cg-standort">
                <div class="cg-inner">
                  <span class="cg-ic" set:html={icons.pin}></span>
                  <b>Standort</b>
                  <span class="cg-loc">{c.loc}</span>
                  <button class="cg-maplink" type="button" data-map={c.slug}>Auf der Karte zeigen →</button>
                </div>
                <figcaption class="cg-cap">Standort</figcaption>
              </figure>
              <figure class="cg-slide cg-ausstattung">
                <div class="cg-inner">
                  <b class="cg-atitle">Ausstattung</b>
                  <ul class="cg-chips">
                    {equip(c).map((e) => (
                      <li><span class="cg-chip-ic" set:html={icons[e.ic]}></span>{e.t}</li>
                    ))}
                  </ul>
                </div>
                <figcaption class="cg-cap">Ausstattung</figcaption>
              </figure>
              <div class="cg-dots" role="tablist" aria-label="Bild wählen">
                <button class="cg-dot is-on" type="button" aria-label="Fahrzeug" aria-selected="true"></button>
                <button class="cg-dot" type="button" aria-label="Standort" aria-selected="false"></button>
                <button class="cg-dot" type="button" aria-label="Ausstattung" aria-selected="false"></button>
              </div>
            </div>
            <div class="car-body">
              <h3>{c.name}</h3>
              <p class="car-type">{c.type}</p>
              <ul class="car-meta">
                <li><span aria-hidden="true">👤</span> {c.seats}</li>
                <li><span aria-hidden="true">⚙️</span> {c.schaltung}</li>
                <li><span aria-hidden="true">💶</span> {c.tarif}</li>
              </ul>
              <button class="car-loc" type="button" data-map={c.slug}>
                <span aria-hidden="true">📍</span> <span class="car-loc-txt">{c.loc}</span><span class="car-loc-go">Karte →</span>
              </button>
            </div>
          </article>
        ))}
        <article class="car car-coop">
          <div class="coop-inner">
            <h3>Kooperation mit Vaterstetten</h3>
            <p>Als Mitglied nutzt du zusätzlich die Fahrzeuge der Vaterstettener Auto-Teiler — beidseitig.</p>
            <a class="btn btn-yellow" href="#kooperation">Mehr zur Kooperation</a>
          </div>
        </article>
      </div>
      <p class="note">Je Fahrzeug eine ruhige Galerie (Fahrzeug · Standort · Ausstattung) — beim Klick bleibt das Bild stehen. Die Fahrzeugdaten sind echt; Standort- und Ausstattungsfotos folgen in Phase 2.</p>
    </div>
  </section>

  <!-- ============ STANDORTE-KARTE ============ -->
  <StandorteKarte />

  <!-- ============ PREISE + KOSTENRECHNER ============ -->
  <section id="preise" class="sec sec-white">
    <div class="wrap">
      <header class="sec-head reveal">
        <h2>Was kostet mich das?</h2>
        <p>Du zahlst nur, was du fährst: Zeit plus Kilometer. Kein Monatsbeitrag, keine versteckten Gebühren.</p>
      </header>

      <blockquote class="pay-claim reveal">
        <p>Gezahlt wird nur für das, was auch tatsächlich gefahren wurde. Und das ist bei uns
          erstaunlich wenig. Wir haben <b>keine Jahresgebühr und keine Monatsgebühr</b> — nur wer
          fährt, zahlt auch. <b>Versicherung und Sprit sind in den Preisen bereits enthalten.</b></p>
      </blockquote>

      <div class="price-grid">
        <div class="card price-table">
          <h3>Tarife im Überblick</h3>
          <table>
            <tbody>
              <tr><td>Zeittarif tagsüber (8–20 Uhr)</td><td>ab 1,00 €/h</td></tr>
              <tr><td>Zeittarif nachts (20–8 Uhr)</td><td>ab 0,20 €/h</td></tr>
              <tr><td>Kilometer inkl. Sprit (Kleinwagen)</td><td>ab 0,40 €/km</td></tr>
              <tr><td>Kilometer inkl. Sprit (E-Auto / größer)</td><td>ab 0,45 €/km</td></tr>
              <tr><td>Kilometer inkl. Sprit (Kleinbus, 9 Sitze)</td><td>ab 0,60 €/km</td></tr>
              <tr><td>Aufnahmebeitrag (einmalig)</td><td>50 €</td></tr>
              <tr><td>Einlage — je Haushalt einmal (zurück bei Austritt)</td><td>600 €</td></tr>
              <tr><td>Monatsbeitrag</td><td><span class="free">0 € · keiner</span></td></tr>
            </tbody>
          </table>
          <p class="fine">Stand 04/2026. Versicherung <b>und Sprit inklusive</b> (SB Haftpflicht 200 € / Kasko 600 €). Die Tankkarte liegt im Fahrzeug — Tanken kostet dich keinen Cent extra.</p>
        </div>

        <div class="card calc">
          <h3>Kostenrechner</h3>
          <label for="calc-h">Dauer: <span id="calc-hv" class="val">3 Std.</span> tagsüber</label>
          <input id="calc-h" type="range" min="1" max="24" value="3" step="1" />
          <label for="calc-km">Strecke: <span id="calc-kmv" class="val">30 km</span></label>
          <input id="calc-km" type="range" min="5" max="300" value="30" step="5" />
          <label for="calc-cls">Fahrzeug</label>
          <select id="calc-cls">
            <option value="0.40">Kleinwagen (Corsa / Sandero) – 0,40 €/km</option>
            <option value="0.45">E-Auto / Clio – 0,45 €/km</option>
            <option value="0.60">Kleinbus 9-Sitzer (Trafic) – 0,60 €/km</option>
          </select>
          <div class="calc-result"><span>Beispielpreis</span><b id="calc-total">7,50 €</b></div>
          <p class="fine">Überschlag: Zeittarif (ab 1 €/h tagsüber) + Kilometer — <b>Versicherung und Sprit sind schon drin</b>. Die Tankkarte liegt im Fahrzeug, Tanken kostet dich keinen Cent extra. Echte Abrechnung übers Buchungssystem.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ============ WARUM TEILEN (Vorteil vs. eigenes Auto) ============ -->
  <section id="vorteil" class="sec">
    <div class="wrap">
      <header class="sec-head reveal">
        <h2>Teilen statt besitzen — spart Geld und Aufwand</h2>
        <p>Ein eigenes Auto steht die meiste Zeit ungenutzt herum, kostet aber jeden Monat. Beim Carsharing zahlst du nur, wenn du wirklich fährst — um alles andere kümmert sich der Verein.</p>
      </header>

      <div class="vs reveal">
        <div class="vs-col vs-own">
          <span class="vs-tag">Eigenes Auto</span>
          <ul>
            <li>Anschaffung, Wertverlust, Reparaturen</li>
            <li>Versicherung, Steuer, TÜV, Wartung</li>
            <li>Tanken, Reifenwechsel, Stellplatz</li>
            <li>Aufwand: alles selbst organisieren</li>
          </ul>
        </div>
        <div class="vs-col vs-share">
          <span class="vs-tag vs-tag-on">Carsharing bei ATG</span>
          <ul>
            <li>Nur zahlen, was du fährst — ab 1 €/h</li>
            <li>Versicherung, Sprit &amp; Wartung inklusive</li>
            <li>Tankkarte liegt im Auto — nichts auslegen</li>
            <li>Kein Aufwand — der Verein macht den Rest</li>
          </ul>
        </div>
      </div>

      <div class="perks">
        <article class="perk reveal">
          <span class="perk-ic" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2"/><path d="M16 2v4M8 2v4M3 10h18"/><path d="m9 16 2 2 4-4"/></svg>
          </span>
          <h3>Flexibel &amp; schnell buchen</h3>
          <p>Fahrzeug in Sekunden reservieren — am Computer, Tablet oder Handy, rund um die Uhr.</p>
        </article>
        <article class="perk reveal">
          <span class="perk-ic" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="5" width="20" height="14" rx="2"/><path d="M2 10h20"/><path d="M6 15h4"/></svg>
          </span>
          <h3>Zutritt per Transponderkarte</h3>
          <p>Kein Schlüsseltausch: Mit deiner Transponderkarte öffnest du jedes gebuchte Fahrzeug.</p>
        </article>
        <article class="perk reveal">
          <span class="perk-ic" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><path d="M9 22V12h6v10"/></svg>
          </span>
          <h3>Ein Haushalt, eine Einlage</h3>
          <p>Je Haushalt zählt eine Mitgliedschaft — nur einmal 600 € Einlage, egal wie viele Fahrer.</p>
        </article>
      </div>
    </div>
  </section>

  <!-- ============ KOOPERATION ============ -->
  <section id="kooperation" class="sec">
    <div class="wrap">
      <header class="sec-head reveal">
        <h2>Gemeinsam mit den Vaterstettener Auto-Teilern</h2>
        <p>Zwei Vereine, eine Idee: Mitglieder nutzen die Fahrzeuge beidseitig — so steht eine gemeinsame Flotte zur Verfügung.</p>
      </header>
      <div class="coop-grid">
        <div class="card">
          <h3>Zwei Vereine, eine Idee</h3>
          <p class="muted-p">In Neukeferloh stehen die ATG-Fahrzeuge. Über die Kooperation mit den Vaterstettener Auto-Teilern nutzt du zusätzlich deren Fahrzeuge — als Mitglied buchst du auf beiden Seiten.</p>
          <a class="btn btn-ghost" href="https://www.carsharing-vaterstetten.de/" rel="noopener">Zu den Vaterstettener Auto-Teilern</a>
        </div>
        <div class="card card-dark">
          <h3>Beidseitig fahren</h3>
          <p>Mehr Auswahl, mehr Verfügbarkeit: Deine Mitgliedschaft gilt auf beiden Seiten der Kooperation.</p>
          <small>Gemeinnützig organisiert · faire Vereinstarife statt teurem Anbieter.</small>
        </div>
      </div>
    </div>
  </section>

  <!-- ============ FAQ ============ -->
  <section class="sec sec-white">
    <div class="wrap wrap-narrow">
      <header class="sec-head reveal">
        <h2>Häufige Fragen</h2>
      </header>
      <div class="faq">
        {faqs.map((f, i) => (
          <details open={i === 0}>
            <summary>{f.q}</summary>
            <div class="faq-a">{f.a}</div>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- ============ MITGLIED WERDEN (CTA-Band) ============ -->
  <section id="mitglied" class="band">
    <div class="wrap band-inner">
      <h2>Möchtest du Mitglied werden?</h2>
      <p>Werd Teil von Carsharing dahoam in Grasbrunn. Wir melden uns persönlich bei dir — einfach und unverbindlich.</p>
      <div class="cta-row band-cta">
        <a class="btn btn-on-dark" href="mailto:info@atg-grasbrunn.de?subject=Mitglied%20werden%20bei%20ATG">E-Mail schreiben</a>
        <a class="btn btn-yellow" href="tel:+498921541339">089 21541339 anrufen</a>
        <a class="btn btn-outline-light" href="mailto:info@atg-grasbrunn.de?subject=R%C3%BCckruf%20gew%C3%BCnscht&body=Bitte%20ruft%20mich%20zu%20folgender%20Zeit%20an%3A%20">Rückruf anfordern</a>
      </div>
      <p class="band-fine">Phase 2: sicheres Online-Antragsformular (DSGVO-konform, verschlüsselt) mit Dokumenten-Upload.</p>
    </div>
  </section>

  <!-- Hero-Slider: Crossfade + Ken-Burns, Auto-Rotation, Dots, Tag-Wechsel -->
  <script>
    const heroSlideEls = document.querySelectorAll<HTMLElement>('.hero-slide');
    const heroDots = document.querySelectorAll<HTMLButtonElement>('.hero-dot');
    const heroTag = document.getElementById('hero-tag');
    const heroH1a = document.getElementById('hero-h1a');
    const heroH1b = document.getElementById('hero-h1b');
    const heroLead = document.getElementById('hero-lead');
    const heroInner = document.querySelector<HTMLElement>('.hero-inner');
    if (heroSlideEls.length > 1) {
      const reduceMo = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
      let cur = 0;
      let timer = 0;
      const apply = (el: HTMLElement) => {
        if (heroH1a) heroH1a.textContent = el.getAttribute('data-h1a') || '';
        if (heroH1b) heroH1b.textContent = el.getAttribute('data-h1b') || '';
        if (heroLead) heroLead.innerHTML = el.getAttribute('data-lead') || '';
        if (heroTag) heroTag.textContent = el.getAttribute('data-tag') || '';
      };
      const show = (n: number) => {
        cur = (n + heroSlideEls.length) % heroSlideEls.length;
        heroSlideEls.forEach((s, i) => s.classList.toggle('is-active', i === cur));
        heroDots.forEach((d, i) => {
          d.classList.toggle('is-on', i === cur);
          d.setAttribute('aria-selected', i === cur ? 'true' : 'false');
        });
        const el = heroSlideEls[cur];
        // Text kurz ausblenden → wechseln → einblenden (lesbarer Übergang)
        if (heroInner && !reduceMo) {
          heroInner.classList.add('is-switching');
          setTimeout(() => { apply(el); heroInner.classList.remove('is-switching'); }, 260);
        } else {
          apply(el);
        }
      };
      // Auto-Rotation langsamer (8,5 s) → genug Zeit zum Lesen
      const start = () => { if (!reduceMo) timer = window.setInterval(() => show(cur + 1), 8500); };
      heroDots.forEach((d, i) => d.addEventListener('click', () => { window.clearInterval(timer); show(i); start(); }));
      start();
    }
  </script>

  <!-- Kostenrechner-Logik (clientseitig; Phase 2 ggf. als Insel/Komponente) -->
  <script>
    const h = document.getElementById('calc-h') as HTMLInputElement | null;
    const km = document.getElementById('calc-km') as HTMLInputElement | null;
    const cls = document.getElementById('calc-cls') as HTMLSelectElement | null;
    const hv = document.getElementById('calc-hv');
    const kmv = document.getElementById('calc-kmv');
    const total = document.getElementById('calc-total');
    if (h && km && cls && hv && kmv && total) {
      const eur = (n: number) => n.toLocaleString('de-DE', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) + ' €';
      const calc = () => {
        const hours = parseInt(h.value, 10);
        const dist = parseInt(km.value, 10);
        const perKm = parseFloat(cls.value);
        hv.textContent = hours + ' Std.';
        kmv.textContent = dist + ' km';
        total.textContent = eur(hours * 1.0 + dist * perKm);
      };
      [h, km, cls].forEach((el) => el.addEventListener('input', calc));
      calc();
    }
  </script>

  <!-- Fahrzeug-Galerien: ruhiger Auto-Wechsel; Klick lässt die Galerie stehen -->
  <script>
    const galReduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    document.querySelectorAll<HTMLElement>('[data-gallery]').forEach((gal, gi) => {
      const slides = Array.from(gal.querySelectorAll<HTMLElement>('.cg-slide'));
      const dots = Array.from(gal.querySelectorAll<HTMLButtonElement>('.cg-dot'));
      if (slides.length < 2) return;
      let idx = 0, timer = 0, frozen = false;
      const show = (n: number) => {
        idx = (n + slides.length) % slides.length;
        slides.forEach((s, i) => s.classList.toggle('is-on', i === idx));
        dots.forEach((d, i) => { d.classList.toggle('is-on', i === idx); d.setAttribute('aria-selected', i === idx ? 'true' : 'false'); });
      };
      const stop = () => { if (timer) { window.clearInterval(timer); timer = 0; } };
      const start = () => { if (galReduce || frozen || timer) return; timer = window.setInterval(() => show(idx + 1), 4200); };
      const freeze = () => { frozen = true; stop(); gal.classList.add('is-frozen'); };
      dots.forEach((d, i) => d.addEventListener('click', (e) => { e.stopPropagation(); show(i); freeze(); }));
      gal.addEventListener('click', freeze);
      gal.addEventListener('mouseenter', () => { if (!frozen) stop(); });
      gal.addEventListener('mouseleave', () => { if (!frozen) start(); });
      window.setTimeout(start, gi * 650); // versetzt starten, damit nicht alle gleichzeitig wechseln
    });

    // Standort anklicken → zur Karte scrollen und das Fahrzeug markieren.
    const goToMap = (slug: string) => {
      const map = document.getElementById('standorte');
      if (!map) return;
      const l = (window as any).lenis;
      if (l) l.scrollTo(map, { offset: -72 }); else map.scrollIntoView({ behavior: 'smooth', block: 'start' });
      window.setTimeout(() => window.dispatchEvent(new CustomEvent('atg:highlight', { detail: { slug } })), 450);
    };
    document.querySelectorAll<HTMLElement>('[data-map]').forEach((btn) => {
      btn.addEventListener('click', (e) => { e.stopPropagation(); goToMap(btn.getAttribute('data-map') || ''); });
    });
  </script>

  <style>
    .wrap { max-width: 1200px; margin: 0 auto; padding: 0 clamp(1rem, 4vw, 2rem); }
    .wrap-narrow { max-width: 820px; }
    .sec { padding: clamp(2.8rem, 6vw, 4.8rem) 0; }
    .sec-white { background: var(--color-bg-alt); }
    .sec-head { max-width: 60ch; margin-bottom: 2rem; }
    .sec-head h2 { font-size: clamp(1.8rem, 4vw, 2.7rem); font-weight: 800; margin-bottom: 0.5rem; }
    .sec-head p { color: var(--color-muted); font-size: 1.08rem; }
    .card { background: var(--color-surface); border: 1px solid var(--color-line); border-radius: var(--radius-card); padding: 1.6rem; box-shadow: 0 1px 2px rgba(24,21,19,.05), 0 18px 40px -28px rgba(24,21,19,.4); }
    .muted-p { color: var(--color-muted); }

    /* Hero — Kino-Slider (Vollbild, Crossfade + Ken-Burns) */
    .hero {
      position: relative;
      min-height: 100svh;
      display: grid;
      align-items: center;
      overflow: hidden;
      color: #fff;
      isolation: isolate;
    }
    .hero-bg { position: absolute; inset: 0; z-index: -2; }
    .hero-slide {
      position: absolute; inset: 0;
      background-size: cover; background-position: center;
      opacity: 0; transform: scale(1);
      transition: opacity 1.2s ease;
      will-change: opacity, transform;
    }
    /* Sanfterer Ken-Burns (1.0 → 1.05) — Motive wirken nicht mehr zu stark herangezoomt */
    .hero-slide.is-active { opacity: 1; animation: kenburns 11s ease-out forwards; }
    @keyframes kenburns { from { transform: scale(1); } to { transform: scale(1.05); } }
    .hero-slide-mvv { background-color: #181513; }

    /* Text-Crossfade beim Slide-Wechsel (lesbarer Übergang) */
    .hero-h1, .hero-lead, .hero-tag { transition: opacity 0.28s ease, transform 0.28s ease; }
    .hero-inner.is-switching .hero-h1,
    .hero-inner.is-switching .hero-lead,
    .hero-inner.is-switching .hero-tag { opacity: 0; transform: translateY(10px); }

    /* Mobile: Fahrzeug groß & erkennbar — cover mit Bildmitte (Auto sitzt mittig),
       KEIN Ken-Burns-Zoom (sonst zu stark herangezoomt/abgeschnitten). Text unten. */
    @media (max-width: 640px) {
      .hero { min-height: 82svh; align-items: end; }
      .hero-slide { background-size: cover; background-position: center center !important; }
      .hero-slide.is-active { animation: none; transform: none; }
      .hero-scrim {
        background: linear-gradient(180deg, rgba(20,15,12,.25) 0%, rgba(20,15,12,.2) 30%, rgba(20,15,12,.72) 66%, rgba(20,15,12,.94) 100%);
      }
      .hero-inner { padding-top: 0; padding-bottom: 2.2rem; }
    }
    .hero-scrim {
      position: absolute; inset: 0; z-index: -1;
      background:
        linear-gradient(96deg, rgba(20,15,12,.9) 0%, rgba(20,15,12,.62) 40%, rgba(20,15,12,.2) 72%, rgba(20,15,12,.4) 100%),
        linear-gradient(0deg, rgba(20,15,12,.6) 0%, rgba(20,15,12,0) 42%);
    }
    .hero-inner { position: relative; z-index: 0; padding-top: clamp(3rem, 9vh, 6rem); padding-bottom: clamp(4.5rem, 12vh, 7rem); }
    .hero-kicker { display: inline-flex; align-items: center; gap: 0.5rem; font-weight: 700; font-size: 0.92rem; color: #fff; background: rgba(255,255,255,.13); backdrop-filter: blur(4px); padding: 0.42rem 0.95rem; border-radius: 999px; }
    .hero-kicker .dot { width: 9px; height: 9px; border-radius: 999px; background: var(--color-yellow); box-shadow: 0 0 0 4px rgba(255,196,0,.25); }
    .hero h1 { font-size: clamp(2.9rem, 8.2vw, 5.4rem); font-weight: 800; margin: 1rem 0 0.5rem; letter-spacing: -0.03em; text-shadow: 0 2px 34px rgba(0,0,0,.35); }
    .hero h1 .hl { position: relative; white-space: nowrap; }
    .hero h1 .hl::after { content: ''; position: absolute; left: -2px; right: -2px; bottom: 0.07em; height: 0.32em; background: var(--color-yellow); z-index: -1; border-radius: 3px; }
    .hero-lead { font-size: clamp(1.08rem, 2.2vw, 1.32rem); color: rgba(255,255,255,.92); max-width: 48ch; margin: 0 0 0.9rem; text-shadow: 0 1px 16px rgba(0,0,0,.45); }
    .hero-lead strong { color: #fff; }
    .hero-tag { display: inline-block; font-family: var(--font-display); font-weight: 700; font-size: 1rem; color: var(--color-yellow); margin-bottom: 1.5rem; min-height: 1.4em; }
    .cta-row { display: flex; flex-wrap: wrap; gap: 0.75rem; }
    .trust { list-style: none; padding: 0; margin: 1.6rem 0 0; display: flex; flex-wrap: wrap; gap: 0.6rem 1.4rem; font-weight: 600; font-size: 0.95rem; color: #fff; }
    .trust li { display: flex; align-items: center; gap: 0.45rem; text-shadow: 0 1px 10px rgba(0,0,0,.4); }
    .trust .ck { display: inline-grid; place-items: center; width: 22px; height: 22px; border-radius: 999px; background: var(--color-yellow); color: var(--color-ink); font-size: 0.8rem; font-weight: 800; }

    .hero-dots { position: absolute; z-index: 1; left: 50%; transform: translateX(-50%); bottom: 1.4rem; display: flex; gap: 0.5rem; }
    .hero-dot { width: 34px; height: 5px; border-radius: 999px; border: 0; background: rgba(255,255,255,.42); cursor: pointer; padding: 0; transition: background 0.3s ease, width 0.3s ease; }
    .hero-dot.is-on { background: var(--color-yellow); width: 50px; }
    .hero-scrollcue { position: absolute; z-index: 1; right: clamp(1rem, 4vw, 2rem); bottom: 1.2rem; width: 30px; height: 48px; border: 2px solid rgba(255,255,255,.5); border-radius: 999px; display: none; }
    .hero-scrollcue span { position: absolute; left: 50%; top: 8px; width: 4px; height: 8px; border-radius: 999px; background: #fff; transform: translateX(-50%); animation: scrollcue 1.8s ease-in-out infinite; }
    @keyframes scrollcue { 0%, 100% { opacity: 0; transform: translate(-50%, 0); } 50% { opacity: 1; transform: translate(-50%, 13px); } }
    @media (min-width: 760px) { .hero-scrollcue { display: block; } }

    @media (prefers-reduced-motion: reduce) {
      .hero-slide.is-active { animation: none; transform: none; }
      .hero-slide { transition: opacity 0.3s ease; transform: none; }
      .hero-scrollcue span { animation: none; opacity: 1; }
    }

    /* Stat-Leiste */
    .stats { background: var(--color-ink); color: #fff; }
    .stats-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 1.4rem 1rem; padding: clamp(1.8rem, 4vw, 2.6rem) clamp(1rem, 4vw, 2rem); }
    @media (min-width: 720px) { .stats-grid { grid-template-columns: repeat(4, 1fr); } }
    .stat b { display: block; font-family: var(--font-display); font-size: clamp(2rem, 5vw, 2.8rem); font-weight: 800; color: var(--color-yellow); line-height: 1; }
    .stat span { font-size: 0.92rem; color: #cfc7bb; }

    /* Steps */
    .steps { display: grid; gap: 1rem; grid-template-columns: 1fr; }
    @media (min-width: 760px) { .steps { grid-template-columns: repeat(3, 1fr); } }
    .step { background: var(--color-bg); border: 1px solid var(--color-line); border-radius: var(--radius-card); padding: 1.5rem; }
    .step-num { display: inline-grid; place-items: center; width: 44px; height: 44px; border-radius: 13px; background: var(--color-red); color: #fff; font-family: var(--font-display); font-weight: 800; font-size: 1.25rem; margin-bottom: 0.8rem; }
    .step h3 { font-size: 1.22rem; margin-bottom: 0.35rem; }
    .step p { color: var(--color-muted); }

    /* Flotte */
    .cars { display: grid; gap: 1.1rem; grid-template-columns: 1fr; }
    @media (min-width: 600px) { .cars { grid-template-columns: repeat(2, 1fr); } }
    @media (min-width: 980px) { .cars { grid-template-columns: repeat(3, 1fr); } }
    .car { background: var(--color-surface); border: 1px solid var(--color-line); border-radius: var(--radius-card); overflow: hidden; box-shadow: 0 14px 34px -26px rgba(24,21,19,.5); transition: transform 0.15s ease; }
    .car:hover { transform: translateY(-3px); }
    .car-photo { position: relative; aspect-ratio: 3 / 2; background: var(--color-bg); }
    .car-photo img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.55s cubic-bezier(0.16, 1, 0.3, 1); }
    .car:hover .car-photo img { transform: scale(1.07); }
    .car-badge { position: absolute; top: 0.7rem; left: 0.7rem; background: var(--color-yellow); color: var(--color-ink); font-weight: 800; font-size: 0.74rem; padding: 0.25rem 0.6rem; border-radius: 999px; }
    .car-body { padding: 1rem 1.1rem 1.2rem; }
    .car-body h3 { font-size: 1.2rem; }
    .car-type { color: var(--color-red-deep); font-weight: 700; font-size: 0.86rem; margin: 0.15rem 0 0.5rem; }
    .car-loc { color: var(--color-muted); font-size: 0.92rem; }
    .car-coop { background: var(--color-ink); color: #fff; display: grid; }
    .coop-inner { padding: 1.4rem; display: flex; flex-direction: column; justify-content: center; gap: 0.6rem; }
    .car-coop h3 { color: #fff; }
    .car-coop p { color: #cfc7bb; font-size: 0.95rem; }
    .car-coop .btn { align-self: start; }
    .note { margin-top: 1.2rem; font-size: 0.86rem; color: var(--color-muted); }

    /* Fahrzeug-Galerie (Fahrzeug · Standort · Ausstattung) */
    .car-gallery { position: relative; aspect-ratio: 3 / 2; background: var(--color-ink); overflow: hidden; cursor: pointer; }
    .cg-slide { position: absolute; inset: 0; margin: 0; opacity: 0; visibility: hidden; transition: opacity 0.8s ease; }
    .cg-slide.is-on { opacity: 1; visibility: visible; }
    .cg-photo { background-size: cover; background-position: center; transition: opacity 0.8s ease, transform 0.6s ease; }
    .car:hover .cg-photo.is-on { transform: scale(1.05); }
    .cg-cap { position: absolute; left: 0.7rem; bottom: 0.7rem; background: rgba(24, 21, 19, 0.72); color: #fff; font-size: 0.72rem; font-weight: 700; padding: 0.2rem 0.55rem; border-radius: 999px; backdrop-filter: blur(3px); }
    .cg-standort, .cg-ausstattung { display: grid; place-items: center; padding: 1rem; color: #fff; }
    .cg-standort { background: linear-gradient(150deg, var(--color-red) 0%, var(--color-red-deep) 100%); }
    .cg-ausstattung { background: linear-gradient(150deg, var(--color-ink) 0%, var(--color-ink-soft) 100%); }
    .cg-inner { text-align: center; display: grid; gap: 0.5rem; justify-items: center; width: 100%; }
    .cg-ic { display: grid; place-items: center; color: var(--color-yellow); }
    .cg-ic svg { width: 34px; height: 34px; }
    .cg-standort b, .cg-ausstattung b { font-family: var(--font-display); font-size: 1.05rem; }
    .cg-loc { font-size: 0.92rem; color: rgba(255, 255, 255, 0.92); max-width: 24ch; line-height: 1.3; }
    .cg-maplink { margin-top: 0.35rem; background: var(--color-yellow); color: var(--color-ink); border: 0; border-radius: 999px; font-family: var(--font-body); font-weight: 800; font-size: 0.8rem; padding: 0.42rem 0.85rem; cursor: pointer; min-height: 38px; }
    .cg-maplink:hover { background: var(--color-yellow-deep); }
    .cg-chips { list-style: none; margin: 0.2rem 0 0; padding: 0; display: grid; grid-template-columns: 1fr 1fr; gap: 0.45rem 0.7rem; text-align: left; }
    .cg-chips li { display: flex; align-items: center; gap: 0.45rem; font-size: 0.8rem; font-weight: 600; color: #fff; }
    .cg-chip-ic { display: grid; place-items: center; width: 24px; height: 24px; border-radius: 7px; background: rgba(255, 255, 255, 0.12); color: var(--color-yellow); flex: none; }
    .cg-chip-ic svg { width: 15px; height: 15px; }
    .cg-dots { position: absolute; right: 0.6rem; bottom: 0.6rem; display: flex; gap: 0.3rem; z-index: 2; }
    .cg-dot { width: 8px; height: 8px; border-radius: 999px; border: 0; background: rgba(255, 255, 255, 0.5); cursor: pointer; padding: 0; transition: width 0.3s ease, background 0.3s ease; }
    .cg-dot.is-on { background: var(--color-yellow); width: 20px; }

    /* Standort-Button unter der Karte (springt zur Karte) */
    .car-loc { display: flex; align-items: center; gap: 0.35rem; width: 100%; margin-top: 0.6rem; background: var(--color-bg); border: 1px solid var(--color-line); border-radius: 10px; padding: 0.5rem 0.7rem; color: var(--color-ink); font: inherit; font-size: 0.9rem; font-weight: 600; cursor: pointer; text-align: left; transition: border-color 0.15s ease, background 0.15s ease; }
    .car-loc:hover { border-color: var(--color-red); background: var(--color-red-soft); }
    .car-loc-txt { min-width: 0; }
    .car-loc-go { margin-left: auto; color: var(--color-red-deep); font-weight: 800; font-size: 0.82rem; white-space: nowrap; }

    /* Warum teilen — Vergleich + Perks */
    .vs { display: grid; gap: 1rem; grid-template-columns: 1fr; margin-bottom: 1.6rem; }
    @media (min-width: 760px) { .vs { grid-template-columns: 1fr 1fr; } }
    .vs-col { border: 1px solid var(--color-line); border-radius: var(--radius-card); padding: 1.3rem 1.4rem; background: var(--color-surface); }
    .vs-share { border-color: var(--color-red); box-shadow: 0 18px 40px -28px rgba(212, 42, 34, 0.5); }
    .vs-tag { display: inline-block; font-weight: 800; font-size: 0.78rem; text-transform: uppercase; letter-spacing: 0.05em; color: var(--color-muted); margin-bottom: 0.8rem; }
    .vs-tag-on { color: #fff; background: var(--color-red); padding: 0.28rem 0.75rem; border-radius: 999px; }
    .vs-col ul { list-style: none; margin: 0; padding: 0; display: grid; gap: 0.55rem; }
    .vs-col li { position: relative; padding-left: 1.6rem; color: var(--color-ink); font-size: 0.96rem; line-height: 1.35; }
    .vs-own li { color: var(--color-muted); }
    .vs-own li::before { content: '✕'; position: absolute; left: 0; color: var(--color-muted); font-weight: 800; }
    .vs-share li::before { content: '✓'; position: absolute; left: 0; color: var(--color-red); font-weight: 800; }
    .perks { display: grid; gap: 1rem; grid-template-columns: 1fr; }
    @media (min-width: 760px) { .perks { grid-template-columns: repeat(3, 1fr); } }
    .perk { background: var(--color-surface); border: 1px solid var(--color-line); border-radius: var(--radius-card); padding: 1.3rem; }
    .perk-ic { display: grid; place-items: center; width: 48px; height: 48px; border-radius: 13px; background: var(--color-red-soft); color: var(--color-red-deep); margin-bottom: 0.7rem; }
    .perk-ic svg { width: 26px; height: 26px; }
    .perk h3 { font-size: 1.15rem; margin-bottom: 0.3rem; }
    .perk p { color: var(--color-muted); font-size: 0.95rem; }

    /* Preise + Rechner */
    .price-grid { display: grid; gap: 1.1rem; grid-template-columns: 1fr; }
    @media (min-width: 880px) { .price-grid { grid-template-columns: 1.1fr 1fr; } }
    .price-table h3, .calc h3 { font-size: 1.3rem; margin-bottom: 0.8rem; }
    .price-table table { width: 100%; border-collapse: collapse; font-size: 0.97rem; }
    .price-table td { padding: 0.55rem 0; border-bottom: 1px solid var(--color-line); }
    .price-table td:last-child { text-align: right; font-weight: 700; font-variant-numeric: tabular-nums; }
    .price-table tr:last-child td { border-bottom: 0; }
    .free { background: #167a4e; color: #fff; font-weight: 800; font-size: 0.78rem; padding: 0.2rem 0.6rem; border-radius: 999px; }
    .fine { font-size: 0.82rem; color: var(--color-muted); margin-top: 0.9rem; }
    .calc { background: var(--color-ink); color: #fff; }
    .calc h3 { color: #fff; }
    .calc label { display: block; font-weight: 700; margin: 0.9rem 0 0.3rem; font-size: 0.95rem; }
    .calc input[type='range'] { width: 100%; accent-color: var(--color-yellow); }
    .calc .val { color: var(--color-yellow); font-weight: 800; font-variant-numeric: tabular-nums; }
    .calc select { width: 100%; padding: 0.6rem; border-radius: 10px; border: 1px solid rgba(255,255,255,.2); background: var(--color-ink-soft); color: #fff; font: inherit; min-height: 44px; }
    .calc-result { margin-top: 1.2rem; background: var(--color-red); border-radius: 12px; padding: 1rem 1.2rem; display: flex; justify-content: space-between; align-items: baseline; }
    .calc-result b { font-family: var(--font-display); font-size: 2rem; }
    .calc .fine { color: #cfc7bb; }

    /* Kooperation */
    .coop-grid { display: grid; gap: 1.1rem; grid-template-columns: 1fr; }
    @media (min-width: 820px) { .coop-grid { grid-template-columns: 1fr 1fr; } }
    .card-dark { background: var(--color-ink); color: #fff; }
    .card-dark h3 { color: #fff; }
    .card-dark small { color: #b3a99c; }
    .card h3 { font-size: 1.25rem; margin-bottom: 0.6rem; }
    .card .btn { margin-top: 0.4rem; }

    /* FAQ */
    .faq details { background: var(--color-surface); border: 1px solid var(--color-line); border-radius: 12px; margin-bottom: 0.7rem; overflow: hidden; }
    .faq summary { cursor: pointer; font-weight: 700; padding: 1rem 1.2rem; list-style: none; display: flex; justify-content: space-between; gap: 1rem; align-items: center; }
    .faq summary::-webkit-details-marker { display: none; }
    .faq summary::after { content: '+'; font-family: var(--font-display); font-size: 1.5rem; color: var(--color-red); }
    .faq details[open] summary::after { content: '–'; }
    .faq-a { padding: 0 1.2rem 1.1rem; color: var(--color-muted); }

    /* CTA-Band */
    .band { background: linear-gradient(135deg, var(--color-red) 0%, var(--color-red-deep) 100%); color: #fff; }
    .band-inner { padding: clamp(2.6rem, 6vw, 4.2rem) clamp(1rem, 4vw, 2rem); text-align: center; }
    .band h2 { font-size: clamp(1.9rem, 5vw, 2.9rem); font-weight: 800; max-width: 18ch; margin: 0 auto 0.7rem; }
    .band p { max-width: 48ch; margin: 0 auto 1.4rem; color: #ffe7e3; }
    .band-cta { justify-content: center; }
    .band-fine { font-size: 0.84rem; color: #ffd6d0; margin-top: 1.1rem; }

    /* Sektions-Eyebrow */
    .kicker { display: inline-flex; align-items: center; gap: 0.5rem; font-weight: 800; font-size: 0.8rem; letter-spacing: 0.08em; text-transform: uppercase; color: var(--color-red-deep); margin-bottom: 0.5rem; }
    .kicker .dot { width: 8px; height: 8px; border-radius: 999px; background: var(--color-yellow); }

    /* KFZ-Meta (Sitze/Tarif) */
    .car-meta { list-style: none; padding: 0; margin: 0.45rem 0 0.55rem; display: flex; flex-wrap: wrap; gap: 0.35rem 1rem; }
    .car-meta li { font-size: 0.85rem; color: var(--color-ink); font-weight: 600; display: inline-flex; align-items: center; gap: 0.3rem; }

    /* Werbespruch-Callout am Kostenrechner */
    .pay-claim { margin: 0 0 1.6rem; padding: 1.2rem 1.5rem; border-left: 5px solid var(--color-yellow); background: var(--color-surface); border-radius: 0 var(--radius-card) var(--radius-card) 0; box-shadow: 0 14px 34px -26px rgba(24,21,19,.5); }
    .pay-claim p { font-size: clamp(1.05rem, 2vw, 1.22rem); line-height: 1.55; color: var(--color-ink); font-weight: 500; }
    .pay-claim b { color: var(--color-red-deep); font-weight: 800; }

    /* ===== Mobile-Feinschliff (an Carsharing-Apps orientiert: weniger scrollen,
       mehr auf einen Blick). Flotte wird zum wisch-baren Karten-Karussell. ===== */
    @media (max-width: 640px) {
      .sec { padding: 2.1rem 0; }
      .sec-head { margin-bottom: 1.3rem; }
      .sec-head h2 { font-size: clamp(1.55rem, 6.5vw, 2rem); }
      .sec-head p { font-size: 1rem; }
      .stats-grid { gap: 1rem 0.8rem; }

      /* Flotte: horizontales Snap-Karussell statt langer Spalte */
      .cars {
        grid-auto-flow: column;
        grid-auto-columns: 82%;
        grid-template-columns: none;
        overflow-x: auto;
        scroll-snap-type: x mandatory;
        gap: 0.9rem;
        margin: 0 calc(-1 * clamp(1rem, 4vw, 2rem));
        padding: 0.2rem clamp(1rem, 4vw, 2rem) 1rem;
        -webkit-overflow-scrolling: touch;
        scrollbar-width: none;
      }
      .cars::-webkit-scrollbar { display: none; }
      .car { scroll-snap-align: start; }
      .car-coop { min-height: 0; }
      .note { margin-top: 0.6rem; }

      /* Vergleich kompakter */
      .vs { gap: 0.7rem; margin-bottom: 1.1rem; }
      .vs-col { padding: 1rem 1.1rem; }
      .perks { gap: 0.7rem; }
    }
  </style>
</Base>
